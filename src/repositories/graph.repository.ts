/*
 * > 知識圖譜的前台檢視與後台管理
 * @ 目前仍轉接 Mock 資料；view 與 component 一律從這裡取用，串接後端時只需改這一層。
 * TODO(api-integration): 對應 GET/POST /api/v2/admin/knowledge-graph。
 */

export * from '@/mocks/graph'
export * from '@/mocks/graphAdmin'
export * from '@/mocks/notebookKnowledgeGraph'
